import { PrivacyPoliciePipe } from './privacy-policie.pipe';

describe('PrivacyPoliciePipe', () => {
  it('create an instance', () => {
    const pipe = new PrivacyPoliciePipe();
    expect(pipe).toBeTruthy();
  });
});
